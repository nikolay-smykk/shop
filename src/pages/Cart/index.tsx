import React from 'react';
import { Layout } from '../../shared/Layout';

import style from './style.module.css';

export const Cart = () => {
  return (
    <Layout>
      <div className={style.wrapper}>Card</div>
    </Layout>
  );
};
