import React from 'react';
import { Layout } from '../../shared/Layout';

import style from './style.module.css';

export const Auth = () => {
  return (
    <Layout>
      <div className={style.wrapper}>Auth Some</div>
    </Layout>
  );
};
